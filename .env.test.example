# Copy to .env.test and fill in real values for local runs.
# In CI, set these as secrets instead.

FEYRN_BASE_URL=http://localhost:8080

# Primary journey user (Phases 01–07)
FEYRN_TEST_USER_EMAIL=testuser@feyrn-test.de
FEYRN_TEST_USER_PASSWORD=TestFeyrn2026!

# Receiver user (Phase 10)
FEYRN_TEST_RECEIVER_EMAIL=receiver@feyrn-test.de
FEYRN_TEST_RECEIVER_PASSWORD=TestFeyrn2026!

# Optional: only needed if a test queries Supabase directly
VITE_SUPABASE_URL=https://advxucizvsqeipznabyu.supabase.co
